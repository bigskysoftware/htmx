---
title: "About"
description: "Learn about htmx and meet the team behind it"
---

import { getCollection } from 'astro:content';
import { Image } from 'astro:assets';

export const team = (await getCollection('team')).map(member => member.data);
export const teamImages = import.meta.glob('/src/assets/img/team/*.png', {eager: true});
export const communityLinks = (await getCollection('community')).map(link => link.data);

## Team

<div class="not-prose grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
    {team.map((member) => (
        <div class="border border-neutral-200 dark:border-neutral-800 bg-white dark:bg-neutral-900/30 rounded-[3px] p-4 flex flex-col items-center text-center">
            <div class="w-20 h-20 rounded-full overflow-hidden mb-3 bg-neutral-200 dark:bg-neutral-800">
                <Image src={teamImages[member.image].default} alt={member.name} class="w-full h-full object-cover" width={160} height={160} />
            </div>
            <h3 class="text-sm font-chicago text-neutral-900 dark:text-neutral-100 mb-1">{member.name}</h3>
            <p class="text-xs text-neutral-500 dark:text-neutral-450 leading-relaxed" set:html={member.content}></p>
        </div>
    ))}
</div>

## Books

<div class="not-prose grid grid-cols-[repeat(auto-fill,minmax(140px,1fr))] gap-8 my-8">
    {[
        {href: "https://hypermedia.systems", img: "/src/assets/img/hypermedia-systems.png", title: "Hypermedia Systems"},
        {href: "https://www.amazon.com/Server-Driven-Web-Apps-htmx-Language/dp/B0D9N35GKP", img: "/src/assets/img/server-driven-web-apps.png", title: "Server-Driven Web Apps with htmx"},
        {href: "https://www.wimdeblauwe.com/books/modern-frontends-with-htmx/", img: "/src/assets/img/modern-frontends-htmx.jpg", title: "Modern frontends with htmx"},
        {href: "https://www.amazon.co.jp/-/en/dp/4297149451/", img: "/src/assets/img/hypermedia-systems-japanese.png", title: "ハイパーメディアシステム (Japanese Edition)"},
        {href: "https://www.amazon.co.jp/-/en/dp/4863544693", img: "/src/assets/img/htmx-japanese.png", title: "HTMX (Japanese)"},
    ].map(book => (
        <a href={book.href} target="_blank" rel="noopener noreferrer"
           class="group block text-center no-underline transition">
            <img src={book.img} alt={book.title} class="w-full h-auto rounded mb-4" />
            <div class="font-bold text-blue-600 dark:text-blue-400 group-interact:underline text-sm leading-tight">{book.title}</div>
        </a>
    ))}
</div>

## Community

<div class="not-prose grid grid-cols-1 md:grid-cols-2 gap-6">
    {communityLinks.map((link) => (
        <a href={link.url}
           target={link.url.startsWith('http') ? "_blank" : undefined}
           rel={link.url.startsWith('http') ? "noopener noreferrer" : undefined}
           class="group border border-neutral-200 dark:border-neutral-800 bg-white dark:bg-neutral-900/30 interact:bg-neutral-100 dark:interact:bg-neutral-800/50 rounded-[3px] p-6 transition-colors flex items-center gap-4 no-underline">
            <div class="shrink-0 flex items-center justify-center">
                <i class={`${link.iconClass} size-8 text-neutral-600 dark:text-neutral-400`}></i>
            </div>
            <div>
                <h3 class="text-lg font-chicago text-neutral-900 dark:text-neutral-100 mb-1 group-interact:text-blue-600 dark:group-interact:text-blue-600 transition">{link.name}</h3>
                <p class="text-sm text-neutral-600 dark:text-neutral-400">{link.description}</p>
            </div>
        </a>
    ))}
</div>

## Resources

- [Interviews](/interviews) — conversations with people building and thinking about hypermedia
- [Podcasts](/podcasts) — talks, interviews, and appearances about htmx
- [Memes](/memes) — htmx memes for hypermedia teens
