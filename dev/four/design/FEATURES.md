# htmx 4.0 new features

# Implemented
* `<partial>` support

# Future
* ootb SSE support
* `<server-action>` support
* preload
  * With the new infrastructure it should be much easier to integrate the preload extension into htmx core
* optimistic
  * With the new infrastructure it should be easy to implement optimistic content replacement, either with template content or script content
